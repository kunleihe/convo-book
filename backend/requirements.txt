fastapi==0.115.0         # Web framework for your backend API
uvicorn==0.31.0          # ASGI server to run FastAPI
websockets==13.1         # For websocket support (real-time features)
python-dotenv==1.0.1     # For loading environment variables from .env files
python-multipart==0.0.9  # For handling form data (file uploads, etc.)
openai==1.51.0           # For OpenAI API integration (if you use it)
sqlalchemy==2.0.36       # ORM for database (SQLite, etc.)
PyYAML==6.0.1            # For YAML file parsing
PyJWT==2.10.1            # For JWT token authentication
psycopg2-binary==2.9.10   # PostgreSQL adapter for Python (for RDS)